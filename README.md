##Handlebar helper for conditional branching based on comparison of exactly 2 values##
execute:
```javascript
node test.js
```
to see it in action.

**important: condition must be passed as text (encosed in quotes, for example'==' or ">=")**
